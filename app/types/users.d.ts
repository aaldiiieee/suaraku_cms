export interface UserTypes {
  mu_id: number;
  mu_uuid: string;
  mu_nik: string;
  mu_fullname: string;
  mu_phoneNumber: string;
  mu_blood_type: string;
  mu_address: string;
  mu_province: string;
  mu_city: string;
  mu_district: string;
  mu_createdAt: Date;
  mu_updatedAt: Date;
}
